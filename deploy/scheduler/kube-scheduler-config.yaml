# 第二调度器配置：kore-webhook 会给绑核 Pod 注入 schedulerName: kore-scheduler
apiVersion: kubescheduler.config.k8s.io/v1
kind: KubeSchedulerConfiguration
leaderElection:
  leaderElect: false
profiles:
- schedulerName: kore-scheduler
  plugins:
    multiPoint:
      enabled:
      - name: Kore
