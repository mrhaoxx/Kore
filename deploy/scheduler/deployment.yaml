apiVersion: apps/v1
kind: Deployment
metadata:
  name: kore-scheduler
  namespace: kore-system
spec:
  replicas: 1
  selector:
    matchLabels: { app: kore-scheduler }
  template:
    metadata:
      labels: { app: kore-scheduler }
    spec:
      serviceAccountName: kore-scheduler
      containers:
      - name: scheduler
        image: docker.io/mrhaoxx/kore-scheduler:v0.2.0   # 按实际镜像仓库修改
        args:
        - --config=/etc/kore/config.yaml
        - --authentication-skip-lookup=true
        volumeMounts:
        - { name: config, mountPath: /etc/kore, readOnly: true }
      volumes:
      - name: config
        configMap: { name: kore-scheduler-config }
